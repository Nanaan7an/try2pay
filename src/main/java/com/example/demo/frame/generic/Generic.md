# 泛型
参数化类型

### 实战
以开商铺为例。
#####  实体类之间的关系
首先要创建一些店铺`Market`，在创建时就决定好这个店铺的类型，不同的店铺出售不同类型的商品`Goods`，如饮品店`Drink`、甜品店`Dessert`等。

如果不属于`Goods`，则不能开这类商铺，也不能出售（商铺的方法），如人口`Human`。

即：只有`Goods`及其子类才可以作为`Market`的类型参数，这限制了`Market`的泛型上界。

#####  泛型类
在定义类时声明`<T>`，表示该类为泛型类，在实例化时必须指定`T`的类型。



